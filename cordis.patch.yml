# dsh-web-search-anysearch bundle patch: inserts the AnySearch search provider
# into the web plugin roster. The bundle is the official dsh plugin shape —
# install it with `dsh plugin --profile <name> add <path-or-git-url>`.
- insert:
    - id: web-search-anysearch
      name: 'dsh-web-search-anysearch'
